import { ReplacePointWithCommaPipe } from './replace-point-with-comma.pipe';

describe('ReplacePointWithCommaPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplacePointWithCommaPipe();
    expect(pipe).toBeTruthy();
  });
});
